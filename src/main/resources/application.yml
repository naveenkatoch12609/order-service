spring:
  application:
    name: order-service

  profiles:
    active: dev
  
  config:
    import: optional:"http://config-server.default.svc.cluster.local:8888"

management:
  endpoints:
    web:
      exposure:
        include: health,info,env